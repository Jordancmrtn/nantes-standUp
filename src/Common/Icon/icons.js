import { ReactComponent as TicketIcon } from './assets/ticketIcon.svg';

const fill = (c) => ({ fill: c });

export const icons = {
  TICKET: {
    Icon: TicketIcon,
    iconStyle: fill
  }
};

export default icons;
